package co.pickcake.chatGPT.query.event;

public enum WeatherEvent {
    /* 유지 보수때문에 string 으로 치환해야할 것 같음. 지금 버전에서는 수정하지말고 아래에만 추가하도록 */
    SUNNY,
    CLOUDY,
    RAINING,
    WINDY,
    CLEAR,
    SNOWING,

}
